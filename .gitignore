# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
*.tfvars
!terraform.tfvars.example

# SSH keys / secrets pulled down locally
*.pem
*.key
inventory.ini
ansible/inventory.ini
.env

# OS / editor
.DS_Store
*.swp

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
